package MetodosLogicos;
import java.util.*;
public class Login {

    public Login(){}
 /*
    public JaExisteAnimalException.User fazerLogin (String username, String passwd, List<JaExisteAnimalException.User> users)throws NaoExisteUserException{
        for(JaExisteAnimalException.User u: users){
            if(u.getUsername().equals(username) && u.getPasswd().equals(passwd)) return u;
        }
        throw new NaoExisteUserException("O seu utilizador é inválido pf. registe-se ou contacte o admin");
    }

    public void addUser(JaExisteAnimalException.User newUser, List<JaExisteAnimalException.User> users)throws JaExisteUserEcxeption{
        boolean found=false;
        for(JaExisteAnimalException.User u: users){
            if(u.getUsername().equals(newUser.getUsername())){
                found=true;
                break;
            }
        }
        if(found)
            throw new JaExisteUserEcxeption("O utilizador já se encontra registado escolha outro nome");
        else
            users.add(newUser);
    }


    public void redirectUser(JaExisteAnimalException.User user){
        if(user instanceof Consulta.UserFuncionario) return;
        else
            if(user instanceof DadosDiag.UserCliente) return;
            else
                if(user instanceof Empresa.UserDonoEmpresa) return; // vai redirecionar para a UI correspondente

                //Fazer na main

    }*/
}
